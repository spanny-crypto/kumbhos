node_modules
.next
out
.env
.env.local
.env*.local
npm-debug.log*
*.tsbuildinfo
next-env.d.ts
.vercel

# Local Netlify folder
.netlify
.next-app
.app-build-parked
mobile/node_modules
mobile/www
mobile/android/build
mobile/android/app/build
mobile/android/.gradle
mobile/.netlify
deno.lock

# Built APK artifact bundled for direct download on the landing page —
# binary build output, not source; rebuild via mobile/README instead of
# tracking it in git history.
landing/downloads/*.apk
